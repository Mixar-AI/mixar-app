<!-- SPDX-FileCopyrightText: 2026 Adeveda Enterprises Private Limited -->
<!-- SPDX-License-Identifier: GPL-2.0-or-later -->

# Mixar

**Mixar is a custom fork of Blender 5.0 that turns Blender into an AI-powered 3D content creation tool**, with deep integrations for texture painting, AI-assisted modeling, and real-time agent chat. It's built as an **overlay system** — Mixar's source code gets layered on top of upstream Blender during build.

## Build & Development

### Build Commands
```bash
make init          # Initialize: git submodules + LFS files
make build         # Full build: overlay + CMake + compile + install packages
make clean_build   # Clean build: removes source/ and rebuilds
make install       # Install Python packages into embedded Blender Python
```

### How the Build Works
- `scripts/unix/build.sh` orchestrates the build
- `scripts/unix/overlay.sh` copies `/src` onto `/source` (Blender upstream)
- CMake install clears the bundled `scripts/mixar` package before recopying it,
  so incremental builds cannot retain Python modules deleted from `/src`
- **Git worktrees build out of the box**: `upstream/` is a multi-GB submodule that linked worktrees don't carry, so `settings.sh` falls back to the main checkout's `upstream/` (read-only rsync source; warns if the shared tree isn't at the commit the branch pins). Override with `MIXAR_UPSTREAM_DIR`. Each worktree still assembles its own `source/` and `build/` — never share those.
- Config loaded from `.env` → env vars via `scripts/unix/settings.sh` → `scripts/generate_config.py` generates runtime `mixar.json` into the app bundle
- C++ env header auto-generated at `source/creator/mixar_env_config.h`
- Python build-frozen env marker auto-generated at `source/scripts/mixar/config/_build_env.py` (gates `get_dev_bypass_credentials`; `DEV_BYPASS_ALLOWED=True` only when `MIXAR_ENV=Dev`). Setting `DEV_BYPASS_*` env vars with `MIXAR_ENV != Dev` aborts the build.
- Python packages installed from `scripts/python_requirements.txt` into embedded Blender Python
- **macOS dev codesign (optional)**: `MIXAR_DEV_SIGN_ID` in `.env` makes `build.sh` re-sign the built `Mixar` executable with a stable self-signed identity (one-time setup: `scripts/unix/setup_dev_codesign.sh`). Without it, every rebuild's ad-hoc signature is a new identity to macOS Keychain, so the "Mixar wants your confidential information" prompt returns after each rebuild and "Always Allow" never sticks; with a stable cert, one "Always Allow" persists. Signs with a `get-task-allow` entitlement to keep lldb attach working. Local-dev only — release signing stays in `package.sh`.
- **Never run `cmake` or `make` directly in `source/`** — always use `make build` or `./scripts/unix/build.sh` (overlay must run first)

### Testing
```bash
python -m pytest -q  # Run the standalone stable suite (default)
python -m pytest -q src/scripts/mixar/modules/testing  # Legacy/embedded suite (requires runtime deps)
```
The default suite runs outside Blender; root `conftest.py` injects `bpy` stubs. The legacy/embedded module suite is explicit because some tests require Blender/runtime dependencies.

### GUI E2E: the QA harness (MISSION-CRITICAL — this is how features ship)
The QA harness drives the REAL built app like a human — semantic clicks by
operator-id/prop/surface (no pixel guessing), drags, file drops, targeted
screenshots you must actually READ — and runs replayable E2E scenarios (agent
chat, moodboard graph, full image→3D→retopology pipeline). **A feature is DONE
only when the running app has proven it: state asserts AND vision, plus a
scenario left behind.** Before any feature work, read the playbook:
- Harness: `/Users/satyam/mixar-src/scripts/qa-gui/` (private repo
  `github.com/Mixar-AI/mixar-qa-harness`) — `README.md` (architecture +
  hard-won gotchas), `SHIP_LOOP.md` (the build→drive→verify→encode→ship
  contract), `UX_CHECKLIST.md` (checkable "looks right" criteria).
- In-app C++ half (introspection RNA, custom-surface targets, drop hook):
  branch `feature/qa-gui-harness` in THIS repo — must be in the Dev build.
- Run: `cd ~/mixar-src/scripts/qa-gui && ./run_qa_app.sh` then
  `python3 driver/qa_client.py status`; full suite `./run_scenarios.sh`
  (spends real credits); MCP tools available as the `mixar-qa` server.
- New custom-drawn UI is unshippable until it exports QA targets
  (`Mixar_qa_register_target_provider` — read the surface's OWN hit-test
  geometry, never duplicate it).

## Code Rules

- Keep the code as modular, readable and performance efficient as possible. No file should be larger than 500 lines of code. Leverage C++ wherever needed to get maximum performance.
- All code goes in `/src` — it gets overlaid onto Blender source during build.
- Write all python code inside `/src/scripts/mixar/modules` in the relevant module. When in doubt, ask for the correct module.
- Place all code which can be used across modules in the `common` folder.
- All C/C++ files should be inside `src/source/blender`.
- Put all environment variables in `.env` (copy `.env.example` as template). Never commit `.env`. Config is generated at build time by `scripts/generate_config.py`.
- Keep properties and operators segregated in different folders.
- Keep all the constants for a module inside a `constants.py` file inside the module root.
- **Always update this CLAUDE.md** when features are added, modified, or deleted — keep module descriptions, architecture tables, and patterns in sync with the actual codebase.

## Bootstrap & Registration

The bootstrap system in `src/scripts/startup/bootstrap/__init__.py` handles module loading in 3 phases:

1. **Package setup** — creates synthetic packages for `/src/scripts/mixar/` (no `__init__.py` needed in subdirs)
2. **Bootstrap modules** — loads `src/scripts/mixar/bootstrap/*.py` (must have `register()`/`unregister()`)
3. **UI modules** — auto-discovers all files in `modules/**/ui/` dirs, loads in time-budgeted batches

**Key rules:**
- Only make `register` and `unregister` functions when needed. Let the fallback mechanism handle registrations — pass the list of classes properly via `classes` tuple.
- UI classes in `ui/` are auto-registered by bootstrap. Properties load first (priority 0), then operators/core (1), then panels/menus/headers (2).
- For cross-directory property dependencies, use the module's `__init__.py` to import in order (see `paint/__init__.py`).

## Project Structure

```
.env.example                       # Environment config template (copy to .env for local dev)
scripts/generate_config.py         # Generates runtime mixar.json at build time from env vars
src/
├── scripts/
│   └── mixar/
│       ├── bootstrap/         # Startup modules (agent_connection, paint_module, etc.)
│       ├── config/            # Logging config
│       └── modules/
│           ├── common/utils   # Shared utilities
│           └── {module_name}/
│               ├── constants.py  # Module constants
│               ├── core/         # Functional and calculation logic
│               └── ui/           # UI elements (auto-discovered)
│                   ├── properties/   # PropertyGroup definitions
│                   ├── operators/    # Operator definitions
│                   ├── panels/      # Panel drawing code
│                   ├── menus/       # Menu definitions
│                   └── lists/       # UIList definitions
└── source/
    └── blender/               # C/C++ extensions
```

### Active Modules
`agent_bubble`, `agent_scene_strip`, `agent_viewport_lock`, `asset_search`, `auth`, `common`, `director`, `hunyuan`, `mesh_segment`, `moodboard`, `onboarding`, `operation_history`, `paint`, `space_mixie`, `space_mixie_chat`, `space_texture_sets`, `texel_density`, `uv_editor`, `workflow`

**onboarding** — first-run feature tour: GPU-rendered cards (`core/card/`), dim film + green highlight border (`core/overlay/`). The three sidebar-step highlights (Image Gen / Model Gen / Retopology) measure the rendered panel extent dynamically via `region.view2d.tot_rect` — a Mixar RNA addition in `rna_screen.cc` exposing the View2D total rect — because catalog-driven tab heights vary per mode/model; the `SIDEBAR_PANEL_H_*` constants are only a fallback for builds without that overlay. Top-left cards (welcome/completion) reserve vertical clearance above the floating Agent Bubble window (`host_resolver.bubble_clearance_height`) so the card scales down instead of being cropped behind the bubble.

---

## Core Architecture

| Layer | Location | Language | Purpose |
|-------|----------|----------|---------|
| **Blender C++ modifications** | `src/source/blender/` | C/C++ | Native editor spaces, auth, chat UI rendering |
| **Python addon** | `src/scripts/mixar/` | Python | All feature modules, UI panels, operators |
| **Backend (separate repo)** | `mixar-backend/` | Python/FastAPI | AI agent, API services, GPU job queues |
| **Upstream Blender** | `upstream/` | Git submodule | Base Blender 5.0 source |

**Build flow**: `upstream/` → copy to `source/` → overlay `src/` on top → CMake build with CUDA/OptiX → custom Blender binary.

---

## Feature Modules

**Moodboard media contract:** The moodboard canvas is theme-independent pure
black with a sparse neutral-gray dot grid whose spacing and filled-disc radius
are fixed in canvas space. Zooming in therefore shows fewer, farther-apart,
larger dots; zooming out shows more, smaller dots. Filled geometry prevents the
subpixel Moire grouping produced by GPU point primitives, and the grid fades
out as its projected dot diameter approaches one pixel to avoid undersampling
bands at extreme zoom-out. Moodboard items may reference
still-image or movie `bpy.types.Image` datablocks. Movies support native file-picker and OS drag/drop
and play directly inside their canvas blocks. The centered play/pause affordance
or a double-click toggles runtime-only playback at the movie's native frame rate.
A pass-through mouse-move handler stops playback at the current frame as soon as
the pointer leaves the originating tile; a narrow redraw timer advances frames without
opening another editor, changing the scene timeline, or persisting playback state.
The canvas also owns a persistent, Flora-inspired inference graph. Image,
video, and 3D blocks use stable per-scene IDs and saved typed links; native
drawing reuses Blender's node-editor Bézier evaluator. Prompt, model, and the
backend catalog's parameter schema render in a compact horizontal screen-space
toolbar immediately above the selected block; it hides when zoom makes the tile
too small and omits Mode when only one usable backend service exists. Before
submission the prompt and Generate control live inside the
otherwise-empty tile. Submission locks the prompt until a result arrives, then
the image/movie or Blender asset preview fills the tile and replaces those
controls; plain Enter in the prompt invokes the same generation operator as the
Generate button. A completed block's Edit & Run Again action returns it to Draft while keeping its
current preview, restores the editable prompt, and submits only after Enter or Generate. Deselecting
hides the toolbar; a draft block whose controls are hidden (deselected, or too small on screen for
the screen-space toolbar — same `MOODBOARD_GRAPH_CONTROLS_MIN_PX_*` thresholds) never renders as an
unexplained empty card: it echoes its typed prompt (ellipsized, display-only clamped read far below
the prompt maxlen) or a centered "Describe what you want to create" hint. Queued/Running/Failed/
Cancelled tiles without a preview use the same centered layout — state headline plus the prompt (or
a retry hint); the small corner state label survives only over an existing preview (Edit & Run
Again keeps the previous result visible). Uploaded stills and movies use the same neutral
rounded tile boundary, compact floating media label, and output-handle language. X, Delete, and
Backspace live in the addon keyconfig so GUI keyconfig reloads cannot disable deletion; they remove
selected imported media or inference/asset nodes, while selected links are only unlinked. Each block exposes backend-typed
media inputs and a `+` output handle rather than one socket per scalar parameter.
Repeatable inputs show connected sockets plus one compatible empty socket per
input group, growing progressively until the backend-defined limit is reached.
Generate Image can start from an empty canvas or selected stills; Generate
Video is hidden when the catalog has no enabled model. Queue results return to
their originating block and remain reusable as downstream inputs. Right-click
continuations create and connect the next block automatically. Segment nodes
and broader graph editing are intentionally deferred.
**Graph safety contracts** (pinned by `tests/moodboard/test_node_graph_hardening.py`):
every graph string the C++ canvas reads declares a `maxlen` (the `GRAPH_*_MAXLEN`
block in `moodboard/constants.py`) strictly below the `MIXIE_GRAPH_*_BUF` stack
buffer it is read into, and reads go through `mixie_rna_string_get_clamped` —
`RNA_string_get` is strcpy-shaped, `maxlen` is enforced only on assignment, and
catalog labels plus agent-written `object_names` reach those buffers during draw.
A node's service/model are read from the saved `service_key_id`/`model_slug`
strings via `node_schema.node_service_key`/`node_model_slug`, never from the
`service_key`/`model` dropdowns: a dynamic `EnumProperty` persists as an index
into its items list, so it repoints at a different model after a catalog reorder
or resolves to `LOADING` when a file opens before the 2s-delayed fetch
(`SKIP_SAVE` does not help — it only suppresses operator-repeat and presets).
`restore_node_selection` re-derives the dropdowns once the catalog loads. Link
draw and link hit-testing share one `moodboard_graph_cache_build` pass; resolving
endpoints per link was O(links × images) with a locked `BKE_image_acquire_ibuf`
in the inner loop, every redraw. The link-drag preview is keyed on
`scene->id.session_uid`, not a static `Scene *`, and region exit resets it.
`media_item_by_id` is read-only — `node_output_type` reaches it from a menu draw,
so id migration runs from the moodboard poll tick and `load_post` instead.
**Node-owned movies play in place.** A generated video is tagged
`embedded_node_id`, which excludes it from `moodboard_find_image_under_mouse` —
so every seam that makes an uploaded movie playable needs a node-aware
counterpart, or the result sits frozen on frame 1 with no way to start it:
`mixie_draw_moodboard_graph.cc` draws the shared
`mixie_draw_moodboard_video_overlay` over movie previews, `graph_select_invoke`
toggles playback on the centred affordance or a double-click **before**
installing the move modal (that branch has no drag threshold and would slide the
card on the same click), and `hovered_video_index_from_event` also resolves node
previews or the hover monitor stops playback on the first mouse-move. All three
key on the `mixie_moodboard_images` index via
`moodboard_find_node_preview_video_under_mouse` — a node index would mismatch
`playback.item_index`. The preview rect has one definition
(`moodboard_graph_node_preview_bounds`) so the play button's pixels and its click
region cannot drift apart. `g_video_playback` is keyed on `Image *` and pruned
against `bmain->images`, because deleting a node frees the movie it owns.
The same "never `selected`" property means **selection-driven actions must
resolve through the owning node**: `media_utils.selected_exportable_media()` is
the one definition of that set, shared by the context menu's Export gate and
`mixie.moodboard_export_images`, so a completed node's result can be saved to
disk. It is deliberately scoped to read-only actions — crop/rotate/flip stay
keyed on direct selection, since editing a result in place would desynchronise
it from the node that produced it.
Existing links can be selected at any zoom using screen-space curve hit-testing
and unlinked with X, Delete, Backspace, or the context menu; unlinking preserves
both endpoint blocks. Canvas box selection includes imported media, text,
inference nodes, and 3D asset nodes while ignoring hidden node-owned preview
media. Backend service/model input metadata projects into stable
typed sockets, including repeatable and total-material limits. Users drag the
offset `+` output handle onto a compatible socket; the saved link is rejected
when its type, capacity, occupancy, duplicate, or cycle validation fails, and
clicking the larger `+` opens a compact backend-gated continuation menu for
Image, Video, and 3D nodes without removing drag-to-connect behavior.
Malformed repeatable contracts fail closed. Generated image/video media are
owned by their inference block and released with it; uploaded source media stay
independent.
Node services, models, typed choices, bounds, defaults, widget hints, ordering,
and `visible_if` rules project from the same moodboard-surface backend catalog
as the sidebar and saved nodes resynchronize after catalog swaps.
Movies remain linked to the
source file (Blender cannot pack movies),
are copied in-app and exported without re-encoding, and expose stream-friendly
source metadata in `moodboard/core/media_utils.py`. The catalog-only Video Gen
panel accepts text-only generation or selected mixed references. Its availability,
models, parameter controls, and reference limits are projected from the backend
DB catalog seed; missing or malformed input limits fail closed instead of using
client defaults. The current seed publishes up to 9 stills, 3 movies, 12 total,
with movies at most 15 seconds combined. It compresses stills,
streams movie files through `POST /job-queue/uploads/{image|video}` without
base64 copies, trusts the backend-parsed duration rather than client metadata,
and submits `video_gen` through the unified queue. Completed videos are moved
from queue temp storage into persistent Mixar data storage and added to the
originating scene's moodboard. The enabled catalog models are Seevio Seedance
2.0 variants; Seedance 2.5 is backend-only and disabled until an official API
contract replaces the requested placeholder. Existing image generation,
masking/crop, system-image-clipboard, chat-attachment, and image-to-3D paths
deliberately filter movies because those services remain still-only.

| Module | What it does |
|--------|-------------|
| **director** | Phase-zero camera directing as a mode-level viewport experience. The native C++ View3D layer (`view3d_director_{state,overlay,timeline*}.cc`) reads registered Director RNA and presents an object-aware tool rail, a top-pinned Capture Keyframe action clear of the camera gate, focused all-native shot/camera/animation/render popups, independent Shot Renders, Moodboard keyframe grouping, and Video Gen actions, plus a poll-driven bottom timeline with a per-camera Shot selector, keyframe markers, playback, immersive mode, collapse, and `F` capture. The redundant floating 3D Editor/Canvas switcher is intentionally absent. The dock is a bottom-aligned `RGN_TYPE_CHANNELS` region, not `RGN_TYPE_FOOTER`, because Blender hard-clamps footers to one header row; the first footer-based build is migrated during View3D initialization. Its Flow-style surface uses an adaptive seconds ruler, `MIXAR_OT_director_scrub` for cancel-safe click-drag playhead positioning, a continuous orange camera strip with keyframe handles, mouse-wheel/pinch zoom, horizontal trackpad/Shift-wheel pan, and smart-zoom fit reset; viewport state remains per-region runtime data. Dragging the draft strip shifts the stored frames and matching native transform/rotation/lens keys plus handles as one undoable operation; locked strips remain read-only. Right-click on the strip or a keyframe marker opens the strip menu (`mixar.director_strip_menu`, RMB dispatch in the timeline UI handler): Delete Keyframe (clicked marker or the beat under the playhead), Split at Playhead (`split_shot` moves later beats into a new shot on the SAME camera — native keys stay put, both shots scope their own preview range, the first half stays active), Clear All Keyframes (confirm-gated, every beat through `remove_beat`), and Remove Shot — whose poll now allows directing (the empty-state overlay takes over when the last shot goes). **Deleting the last reference to a camera purges its Director motion** (`purge_camera_animation` in `core/capture.py`: transform/lens F-curves + handheld modifiers), fired when the last beat is removed or the last shot referencing the camera is removed — per-frame key deletion misses stray keys (native auto-keying, hand edits) and a deleted strip must leave a genuinely static camera; `camera_shared_elsewhere` keeps the motion alive while any other take (locked included) still directs the same camera. **Deletion has two entry points beyond the strip menu:** X/Del/Backspace pressed while the pointer is over the timeline delete the keyframe under the cursor (else the one under the playhead, else the active one) from the native timeline UI handler (`beat_to_delete`) via `mixar.director_remove_beat` — the `mixar.director_block_input` guard swallows those keys over the WINDOW region to protect scene objects but never sees them over the CHANNELS timeline region, so the timeline owns its own deletion; and deleting a camera key from the Dope Sheet/Timeline editors is reconciled by `core/beat_sync.py` (`ui/beat_sync_watch.py` bridge, modeled on `auto_key`): a persistent depsgraph handler watches the native Director key count while directing and, on a genuine deletion (count drops below the beat count — never a MOVE, which keeps the count), a debounce timer prunes the now-orphaned beats through `remove_beat` so no stale orange handle or phantom manifest pose survives — the handler only detects, the timer mutates, because editing scene data inside `depsgraph_update_post` is unsafe. Playback runs a bounded 30 Hz redraw notifier only while animation is active, and fewer than two keyframes is not previewable. `MIXAR_OT_director_pick_camera` switches to the newest take already using a camera or creates a new Shot instead of reassigning the current Shot; the active-shot RNA callback synchronizes `scene.camera`, camera view, and the first-to-last-keyframe preview range. The `F` item lives in the addon `3D View` keymap because GUI keyconfig reload empties custom C-registered keymaps. A poll-gated `mixar.director_block_input` guard absorbs object-editing hotkeys while `is_directing` — but **each guard must live in the keymap Blender dispatches FIRST for that key**, because View3D walks its WINDOW-region handlers head→tail (mode keymaps → "3D View Generic" → "3D View") and a guard parked in a later keymap never sees a key an earlier one binds (addon items are *prepended* only within their own keymap on keyconfig merge): G/R/S + Alt+G/R/S + X/Del/Shift+Del guards sit in the addon "Object Mode" keymap, the N/T chrome-toggle guards in "3D View Generic", and the Shift+S snap-pie guard in "3D View" — because the calm surface hides the sidebar/toolbar and a leaked S was scaling the set mid-shot. Director deliberately binds NO `N`/`O` navigate/precise shortcuts (they can never win against those earlier keymaps) and the camera-frame buttons carry no key hints. `MIXAR_OT_director_navigate` is a modal SUPERVISOR around native `view3d.walk` (its own handler is added after walk starts, so it sees events first and passes them through): pressing Esc snapshots the camera pose before walk's native CANCEL reverts it and re-applies that pose once walk dies (`window.modal_operators` polling) — Esc stops navigation in place instead of discarding the framing, while RMB keeps native revert semantics; every exit warps the cursor back to the viewport center (walk otherwise releases it wherever the grab began), and a green aim reticle draws at region center (`POST_PIXEL`) while the pointer is hidden. Viewport stills (`core/capture.py`) must switch `image_settings.media_type` to `IMAGE` before selecting PNG and restore media_type BEFORE the saved file format — legacy scenes with FFMPEG video output otherwise reject PNG (`enum "PNG" not found in ('FFMPEG')`). Director registers no `VIEW_3D` N-panel; native buttons invoke Python operators so behavior has one owner. **All rail popups are native C++ block popups** — `view3d_director_popup_shot.cc` holds Shots (take list rows via `mixar.director_set_active_shot`, rename field, New Take/Remove/Finish), Camera (direction prompt + adherence + timing + guides — deliberately NO lens/aspect duplication), and Animation (preset pairs + motion length); the Python `director_popovers.py`, its `mixar.director_show_*` openers, the shot UIList, and the `show_shots` prop are deleted. **Shot Renders is native too** (`view3d_director_popup_render.cc`, opened via `uiDefBlockBut` like the lens dropdown): the Beauty/Clay/Depth toggles are `ButType::Row` buttons bound straight to the shot's `render_output_types` enum-flag (native PROP_ENUM_FLAG behavior — pushed while the bit is set, click XORs it), the resolution slider binds `render_resolution_percentage`, and the action row invokes `mixar.director_render_videos`; the Python `render_popover.py` panel and its `mixar.director_show_render` opener are deleted, so NO Python panel/popover remains in the module. The block is `UI_BLOCK_KEEP_OPEN` because kind selection is multi-select (a plain popup dismisses on the first toggle click); the popup closes on click-outside/Esc, and the Render action dismisses it through an explicit `UI_popup_menu_retval_set(UI_RETURN_OK)` button func — KEEP_OPEN would otherwise keep it up after the action too. **The rail is 3 block-popup buttons** (Shots · Moves · Camera, +Animation with a character) — Navigate/Precise were removed from it (artist feedback: duplicated; they live on the camera gate and the timeline dock only). **Precise is hidden from every surface** (gate + dock) until its role is clear to artists — the operator and `navigation_mode` property survive for future surfaces — and **Navigate is a text-only button** (no hand icon: it read as a pan tool); the gate's compact icon-only fallback went with it. `scene.mixar_director` stores Shot records referencing native Scene/Camera data. User-facing sparse Keyframes remain stored in the existing `beats` collection for manifest compatibility; they own stable IDs/frame positions and packed Moodboard stills, while native camera transform/rotation/lens keyframes remain spatial truth. Captures and persisted Beauty Preview, Clay, and normalized Depth H.264 guides share a shot-named Moodboard group. `core/render_outputs.py` renders selected passes asynchronously across the first-to-last keyframe and polls Blender's render-job state before each subsequent pass; `core/render_passes.py` snapshots and restores every temporary scene/render/compositor property. Controls cover WASD Navigate, numeric/gizmo Precise, lens/aspect/timing/composition, preview, direction/adherence, draft/take locking, and a provider-neutral `mixar.camera_direction.v1` manifest. **Artist-feedback round (items 6–13)**: the overlay implementation is split (`view3d_director_overlay.cc` + `view3d_director_overlay_frame.cc` + `view3d_director_overlay_intern.hh`) for the 500-line limit — frame-gate controls live in the `_frame` file, rail/empty-state/context actions plus the shared `director_overlay_*` helpers in the main file. The gate lens button speaks **millimetres, never FOV degrees** ("35mm ▾" / "Orthographic ▾" / "Panoramic ▾"). The Lens, Aspect, and Camera Moves popups are **native C++ popup blocks** (`view3d_director_popup.cc`, opened via `uiDefBlockBut`/`uiDefIconBlockBut` from the gate/rail — the stock Python-layout popovers read as spreadsheet tables with truncated labels and were deleted along with their `mixar.director_show_*` openers): presentation only — every row invokes the Python-owned operators (`mixar.director_set_lens_type` / `set_lens` / `set_aspect` / `camera_move`) with **enum identifiers as the frozen contract** (`RNA_enum_set_identifier`), and sliders bind straight to registered RNA (`camera.lens`, `state.beat_seconds`, shot `handheld*`) via `uiDefButR`, so no behavior lives natively. Lens popup: Perspective/Orthographic/Panoramic segments + named mm presets ("Classic · 35mm", `LENS_PRESET_ITEMS` 18–135) + focal/ortho/panorama value row (`FOV_PRESETS_DEGREES` deleted). **Fix Z** (`state.level_horizon`, default on) levels camera roll via `to_track_quat('-Z','Y')` when Navigate starts — walk adds no roll but preserves existing roll, which stranded artists tilted. **Auto Key** (`state.auto_key`, record toggle beside Capture in C++ — `ICON_RECORD_OFF` flipping to `ICON_RECORD_ON` while armed, mirroring the native timeline's auto-key button rather than a static REC glyph): walk exits auto-capture when the pose changed (supervisor `_auto_capture`); Precise edits are watched by `core/auto_key.py` (depsgraph handler + 0.8s-stillness debounce timer, `ui/auto_key_watch.py` bridge) which **rebaselines on frame changes** so scrubbing/playback never capture, and every `capture_beat` records the pose signature (`mark_captured`) so no pose is double-keyed. The **camera frame is movable**: `ui/operators/frame_ops.py` — `mixar.director_drag_frame` modal writes `region_3d.view_camera_offset` with Blender's native camera-view pan math (pixel delta / (region size × 2·zoom-fac)), wheel scales `view_camera_zoom`, Esc restores; `mixar.director_fit_frame` wraps `view3d.view_center_camera`; both are view-only (enabled on locked takes) as small icon buttons at the gate's top-right. **Camera move presets** (`core/camera_moves.py`, rail button → the native Moves popup in `view3d_director_popup.cc`): Orbit L/R (60°, mid+end beats), Dolly In/Out, Crane Up/Down, Pan L/R — every move funnels through the ordinary `capture_beat` flow (native keys, packed stills, manifest; anchor beat at the current pose unless the playhead already holds one), pivoting on `interest_distance` (median visible-mesh depth ahead). **Handheld is deliberately NOT a preset**: jitter can't be represented by sparse keyframes, so the per-shot `handheld`/`handheld_strength` toggle (same popover) applies named noise F-modifiers ("Mixar Handheld", `core/handheld.py`) to the camera's transform curves — captured keys stay raw/clean while previews, guide videos, and the manifest's `evaluated_get` sampling carry the drift; capture re-applies them (the first capture creates the F-curves) and a new take inherits the parent's setting because it shares the camera. **Character animation presets** (`core/animation_presets.py`, the rail's character mode → real Animation popover): Walk/Run (root travel along local −Y with step bob), Turn L/R/Around, Idle sway — blocking-level object keys grouped "Director Character", duration `state.animation_seconds`; deliberately NOT limb animation (the video model animates bodies; the scene carries the path). The **Director toggle sits in the topbar beside the Engine/Zen mode switch** (`director_header.py` appends to `TOPBAR_MT_editor_menus` after the workflow hook; depressed while directing; `_redraw_director_surface` also tags `Window.global_areas` because the topbar is a global area). The rail highlights the active mode with the Director-green accent pill and groups tools with dividers. **Camera trajectory overlay**: a Python `POST_VIEW` GPU handler (`ui/trajectory_overlay.py` + `core/trajectory.py`) draws the active shot's camera path over the scene while directing — timeline-strip orange polyline, green keyframe dots, timeline-playhead-blue current-position marker, always-on-top — gated by `state.show_trajectory` (default on, the "Path" toggle in the Camera popup's Guides row). Samples come from evaluating location F-curves directly via `anim_curves` (never `scene.frame_set` scrubbing — that would re-evaluate the depsgraph per sample; `fcurve.evaluate` includes handheld modifiers), ≤256 points cached behind an animation signature (beat frames, handheld settings, key counts + value probe); only the playhead marker recomputes per redraw. Continue to Video Gen validates the live catalog image-reference cap, selects exactly the keyframe captures, copies adherence-aware guidance, and focuses the catalog-driven panel without submitting or hardcoding a model. |
| **paint** (largest, 59MB) | Layer-based texture painting system with node trees, modifiers, baking, procedural materials, decals, UDIM, vertex colors, asset export |
| **space_mixie_chat** | AI agent chat interface — JSON-RPC 2.0 over WebSocket, SSE streaming, supervised headless sandbox script execution with Windows-safe parent liveness checks, markdown rendering, and post-response 1–5-star feedback with optional comments. **Reconnect-resume**: the WS client has a recv-liveness watchdog (`WS_LIVENESS_TIMEOUT` — a silently dead TCP connection never errors on send, only recv starvation reveals it) that tears down + auto-reconnects; active-turn scene states deliberately survive WS loss (`on_disconnected` only offlines IDLE/CONNECTING) so resumed tool calls aren't rejected, and the status pill/chat header derive "Reconnecting" from transport liveness instead of scene state. **Transport liveness is a separate, faster signal than `is_connected`**: `is_transport_live` trips on `WS_UI_STALE_THRESHOLD` (~20s) of recv silence, so the UI stops claiming Connected within seconds of a network drop instead of waiting out the 45s teardown watchdog (`is_connected` stays True on a zombie socket until then — send-gating must keep using it, a false "down" there drops work); a 2s main-thread liveness-monitor timer (`ConnectionManager._start_liveness_monitor`) repaints the pill/header on liveness flips, since a silent death produces no scene-state edge to redraw on. A mid-turn SSE loss (fast-detected: backend sends ~15s keepalives, client read timeout 75s) re-attaches via `POST /agent/chat/attach`, which replays the backend's seq-stamped Redis event buffer and follows live until [DONE]; the client dedups the replayed overlap by `seq` (`_last_seq` spans the chat + input streams of one turn, reset on a new chat POST). Never re-POST a lost accepted stream — that starts a duplicate turn. **'@' mention autocomplete** in the composer (docked chat AND Agent Bubble — they share the footer draw): typing `@token` opens a suggestion dropdown above the input listing root objects, materials, and asset-marked datablocks. C++ owns the cursor and interaction — `mixie_chat_mention.cc` (detection/state/geometry/draw) + textedit hooks in `interface_handlers.cc`: each edit publishes the token into `scene.mixie_chat_mention_query` via `RNA_property_update` (same synchronous bridge as the `\x1F` Enter-submit marker); ↑/↓ navigate, Enter/Tab accept, first Esc dismisses, single-click accept + hover highlight via row hit-testing during textedit, and any edit-exit dismisses. Python owns the data: `core/mention_registry.py` (lazy depsgraph-dirty singleton, modeled on the dev scene-graph registry/store) fills `scene.mixie_chat_mention_items`; props live in `ui/properties/mention_props.py`. Accept splices `@Name ` (quoted when the name has spaces) over the WHOLE token through `ui_textedit_insert_buf`'s selection-overwrite path, so focus stays and the cursor lands after the mention. Mentions are deliberately TEXT-ONLY: accepting never touches the viewport selection or active object — selection is the user's semantic input channel to the agent ("the selected item"), and `@Name` in the prompt already identifies the asset unambiguously; a selection-sync variant was tried and removed for making "selected item" prompts ambiguous. The dropdown grows the footer via the same dynamic-height machinery as multiline input. Frozen size contracts: `MENTION_MAX_ITEMS` = `FOOTER_MENTION_MAX_ROWS` = 6, `MENTION_QUERY_MAXLEN` = `MENTION_QUERY_MAX` = 96, `MENTION_INSERT_MAXLEN` 300 < C++ 320-byte insert buffer. **Agent final render**: `mixie_chat.agent_final_render` (`ui/operators/agent_final_render_ops.py`) backs the backend render lane's fire-and-forget `render_scene` tool — applies user-confirmed engine/samples/resolution%/device, starts the interactive render job (`INVOKE_DEFAULT`, UI stays responsive), and on `render_complete` a deferred timer saves the Render Result, imports it into the moodboard (packed into the .blend) and restores every touched setting (`render_cancel` restores too); results travel via `bpy.app.driver_namespace["mixie_agent_final_render"][job_key]` with statuses started/done/error (done = blocking fallback when no window). The ScriptExecutor's post-script handler cleanup exempts these two render handlers by OBJECT IDENTITY (`_exempt_handler_ids` in `core/executor.py`) — they are installed during a sandboxed script but must outlive it, and identity can't be forged the way `__module__` can; the operator also self-heals stale job state (no RENDER job running → restore settings, clear, proceed). Temp stills use the `mixar_render_` prefix so `mixie_chat.prune_render_cache` caps them. **Global-cache gotcha**: `mixie_chat_free()` (the space free callback) clears PROCESS-GLOBAL caches (`g_msg_props` RNA property cache, footer cache) — and it runs for chat spaces inside ANY freed Main, including the temp Main the topbar workspace "+" menu / workspace append reads from startup.blend and immediately frees. The footer cache self-heals via validity flags; the message property cache heals via the `!g_msg_props.initialized` layout-rebuild trigger in `mixie_chat_draw_messages` (without it the render path null-guards every access and draws zero-length text — blank chat/bubble until a resize forces a rebuild). Any new global cache cleared in `mixie_chat_free` needs its own self-heal check on the draw path. **Project Rules**: an "Add Rules" button in the chat header (docked, text; Agent Bubble header, icon-only) toggles a C++-drawn rules overlay in the chat main region (`mixie_chat_rules_{overlay,events,util}.cc` + `mixie_chat_rules_intern.hh`) styled exactly like the past-chats overlay (reuses the HIST_* palette/geometry/helpers from `mixie_chat_history_intern.hh`). The overlay is a RULE LIST, not one big textarea: header + close X, a composer box with a Submit button (Enter submits, Shift+Enter newline; full caret editing with a real selection model — click-drag or Shift+navigation selects, anchor in `rules_sel_anchor`, Ctrl/Cmd+A all, Ctrl+C/X/V copy/cut/paste, type-over replaces), and a scrollable card per rule with a left column stacking an enable/disable toggle pill over an edit (pencil) button, wrapped text (dimmed when disabled), and history-style arm-to-confirm delete X; ONLY the pencil enters edit mode (card body is inert), loading the text into the composer ("Save", accent outline on both). Rules have a SCOPE: each card carries a Global/Project chip (`mixie_chat.rule_set_scope` moves the rule between stores); GLOBAL rules live in `~/.mixar/global_rules.json` (`core/rules_global.py`, same list format + cap, atomic temp-file write) and apply to every .mixar file — the overlay groups the list under "Global"/"This File" section headers, the WM mirror lists globals first, and the index-based rule operators resolve a unified index. ALL store mutations (caps, index resolution, scope moves, WM-mirror refresh) live in `core/rules_api.py` — the single surface shared by the UI operators AND the backend agent's PROJECT_RULES tools (`run_agent_tool` dispatcher, called from the backend script template `project_rules/rules_query.py`; tested in `tests/test_rules_api.py`). New rules are born project-scoped. Data is Python-owned, history-style: `scene.mixie_chat_rules` persists a compact JSON list `[{text, enabled}]` (`core/rules.py:parse_rules/serialize_rules`; legacy plain text parses as one enabled rule), mirrored across scenes on write (rules are per-file, chats per-scene) and capped at `CHAT_RULES_MAXLEN` 10000 = C++ `RULES_TEXT_MAX`/buffer (lockstep). C++ reads the `WindowManager.mixie_chat_rule_entries` mirror (rebuilt by `rules_ops.sync_rule_entries` on open and after every rule op) and mutates ONLY by dispatching `mixie_chat.rule_add/rule_update/rule_toggle/rule_delete`. Visibility: `WindowManager.mixie_chat_rules_visible` (toggled by `mixie_chat.add_rules`, cleared by C++ on ESC/click-away/close X); the rules and history overlays are mutually exclusive — each toggle closes the other, rules is checked first in the region UI handler and drawn last. Wire: `core/rules.py:compose_wire_message` prepends the ENABLED rules' texts (globals first, joined by blank lines — so global edits and scope moves trip the mid-session fingerprint too) to the send that opens a new session (called BEFORE `start_session` in `chat_ops.py` + `quick_prompt_ops.py` — empty `mixie_session_id` is the new-session signal); the visible user bubble keeps the raw prompt. **Mid-session rule changes propagate**: a per-scene fingerprint of the enabled-rules text last sent (`scene.mixie_chat_rules_sent_hash`, SKIP_SAVE, stamped by `mark_rules_sent` AFTER a successful send) is compared on every continuation send — on mismatch the CURRENT complete set is re-sent under a strict supersede header (`RULES_UPDATE_HEADER`; `RULES_REMOVED_NOTE` when cleared). Deliberately NOT a diff: old rules are already in the conversation, and replace-all also covers deletions/disables that have no "new text". Modify/input-response sends never carry rules blocks. New files start rule-less; `get_raw_rules` falls back to any non-empty scene for scenes created after the last edit. |
| **agent_viewport_lock** | "Agent is working" viewport treatment: a breathing green inner-glow halo (SpaceView3D POST_PIXEL draw handler) plus an input-block modal that consumes selection/edit input over the 3D viewport while passing camera navigation. Active while an AGENT-mode turn is executing (BUSY/MODIFYING). Gated on `scene.mixie_chat_active_turn_mode` — the mode the *running* turn was started in, stamped/cleared by `SessionManager.set_state` on active-state edges — not the live `mixie_chat_mode` dropdown, so flipping Agent→Ask→Agent mid-turn can't drop the lock (or raise it for a running Ask turn). **Toasts outrank the lock**: notification toasts draw inside the viewport but are a layer above it, and window modal handlers are dispatched before the region's toast UI handler (`view3d_toast_click.cc`), so the block modal is the ONLY place that can yield to them — it passes a LEFTMOUSE **PRESS** through when `point_in_any_toast_control()` (toast_renderer) says it hit a toast button / close X / link. Press only, and controls only: the C++ handler acts solely on left-press and consumes hits (`WM_UI_HANDLER_BREAK`), whereas passing the release (or a card-body miss) would let a synthesized CLICK reach the select/edit keymaps the lock exists to block. |
| **agent_bubble** | Floating draggable / resizable agent chat bubble overlaid on the 3D viewport. Status pill + composer + expandable history, including the shared post-response rating/comment controls. Bridges to space_mixie_chat backend (ConnectionManager + scene message store) so the agent integration is shared. Pure Python: GPU draw handler + persistent modal operator. **Lifecycle invariants (crash-class fixes)**: bubble/pill wmWindows are NEVER serialized — every WM-layer `BLO_write_file` caller (interactive save, save-startup, AND `WM_autosave_write`) unlinks them via `wm_agent_bubble_windows_unlink_for_write`; files that already carry them (old autosaves) are stripped on read by `wm_file_read_strip_agent_bubble_windows` (wm_files.cc, runs in `wm_file_read_setup_wm_finalize` before the first `WM_check`, GUI only) and excluded from ghost-window winid matching. The cached raw GHOST pointers (`g_bubble_ghostwin`/`g_pill_ghostwin`/`g_host_ghostwin`) are invalidated at BOTH chokepoints: `wm_window_close` (space-based, `ED_agent_bubble_windows_closed`) and `wm_window_free` (pointer-based, `ED_agent_bubble_window_freed` — covers file-load WM replacement that bypasses close). Natively-hidden windows (minimised bubble `SW_HIDE`/`orderOut:`, modal-suppressed docks) are skipped in `wm_draw_update` via `Mixar_WindowIsVisible` (GHOST Win32+Cocoa) — presenting GL into a hidden window is what crashed NVIDIA's driver (`DrvPresentBuffers`) after resume-from-sleep; restore paths must tag a redraw after re-showing (they do — `bubble_force_size_and_refresh` / `NC_WINDOW` notifier in `wm_mixar_floating_docks_restore_after_modal`). Header right side: new-chat button (only when history exists) plus an icon-only reconnect button (reuses `mixie_chat.connect`) shown only when logged in and the session is OFFLINE; the status pill shows "Reconnecting" whenever the WS transport is down while scene state is non-offline (down = `is_transport_live` False, i.e. recv-stale past ~20s — not the slower `is_connected`). **Queue-aware pill** (`ui/header.py:_get_status`, returns a `PillStatus` NamedTuple): unified-queue activity is ORTHOGONAL to the agent turn — the agent enqueues a generation and goes IDLE while it runs — so the pill reports it **only in the branch that used to say "Idle"**. Every state above it (Disconnected / Connecting / Reconnecting / Running / Awaiting Input) is something the user must act on and must never be masked by background work. Text is **what + how long**: one job is named from the catalog ("Image Gen 1:24"), several become a count ("3 jobs 1:24" — naming one would misrepresent the rest), and the elapsed clock is the load-bearing half (slow vs stuck, same reasoning as the download substate's moving byte counts). Data comes from `queue_manager.active_queue_activity() -> QueueActivity(count, label, started_at)` reading the canonical FeatureQueue singletons — NOT the `wm.mixie_queue` mirror, which only reflects the features hand-listed in `_attach_listeners()`. `label` is populated ONLY when exactly one job is active and ONLY from `core/labels.catalog_feature_label()` (empty on a catalog miss → the pill says "Generating" rather than leaking a raw key like `mesh_segment`); `started_at` is the OLDEST active job. **Width is a hard constraint**: the pill window is a fixed 148px (`AGENT_BUBBLE_PILL_WIDTH`), Blender clips overflow mid-word, so the count caps at `9+`, the clock uses `format_elapsed_compact` (`2h+` past the hour — `h:mm:ss` is 3 chars wider), and any label over `_QUEUE_TEXT_BUDGET` (16 chars, e.g. "Mesh Segmentation") degrades to "Generating <clock>". `PillStatus.animate` is False for queue states: the ticking clock IS the animation, and running it alongside the trailing-dot pulse reads as two competing animations. Job naming is shared with the Queue UIList via `job_queue/core/labels.py` — the same job must not be named two different things on two surfaces. **Redraw contract**: the bubble and its minimised pill each live in their OWN wmWindow, so `model_io.QUEUE_SURFACE_AREA_TYPES` includes `AGENT_BUBBLE` (and tags its REGIONS, not just the area — same finding as the chat loader animation). The 0.5s repaint pump that advances the clock is `queue_status_icons`' blink timer, keyed on the ACTIVE set rather than RUNNING_* and started from `FeatureQueue._notify()` (not just the mirror listener, which would leave unlisted queues' clocks frozen at 0:00). Any new queue-state surface must be added to that shared list — tagging only VIEW_3D/MIXIE leaves the pill painting a stale label until the user hovers it. |
| **moodboard** | Reference image boards with lossless in-app and cross-platform system clipboard copy/paste, scene reconstruction, image-to-3D, 360° lookdev, scene generation. Scene Gen Experimental source remains in the tree but its operators, UIList, tab PropertyGroups, scene flags, and queue mirrors are intentionally not registered/exposed. The **Image Gen tab** is catalog-driven: model/style dropdowns source `generation_catalog_cache`, settings past style/model render via `generation_params.draw_service_params()`, and the generate operator builds `payload["params"]` from `collect_params("image_gen", model)` — hardcoded enums remain as the offline/pre-auth fallback. The tab also carries a Mode dropdown for the `image_gen` capability's services (paint-surfaced `brush_gen` is filtered out). **From Blockout (`depth_to_image`) lives under whichever capability the catalog assigns it** — the dedicated **AI Render** tab (capability `ai_render`, `ui/ai_render_drawer.py`) or the Image Gen tab's From Blockout mode — and moving it between them is a DB-only `capability_id` flip: the drawing logic is the SHARED `ui/blockout_drawer.py` (never fold it into one tab's drawer), both host tabs carry a catalog model enum, and `lookdev_scene_ops` reads the model from whichever tab currently hosts the service. It renders the Blockout-to-Render inputs (`tab_lookdev` prompt + fast_mode) plus the catalog Model dropdown/schema params and submits through the EXISTING depth-capture flow (`mixie.lookdev_generate` → `lookdev_generate_from_scene`, payload shape unchanged, model slug + params catalog-resolved with `flux-depth-dev`/`{}` fallback). The **Model Gen tab** (panel `MIXIE_PT_gen_image_to_3d`, bl_category "Model Gen") consolidates the catalog's `model_gen` capability: a Mode dropdown (Image to 3D / Image to 3D Pro / Rapid 3D = services `model_3d`/`image_to_3d`/`hunyuan_rapid`), per-mode Model dropdown + schema params via `draw_capability_selector()`, shared input-image UI, the merged **Multiple Views** section (see Detect Views below — it REPLACED the old Pro-only `scene.hunyuan.pro.multi_views` picker list, which was a second, separate image set that the operator silently discarded whenever a turnaround group existed), and `mixie.model_gen_generate` (`ui/operators/model_gen_ops.py`) which routes by mode through the payload assemblers onto each mode's existing feature queue/scene flag/import hook. Catalog-not-loaded falls back to the legacy Basic/Pro subtab UI (`sidebar_panel_drawers._draw_image_to_3d`); the drawer lives in `ui/model_gen_drawer.py`. **Detect Views + Multiple Views (turnaround / model sheets)**: one uploaded image showing the same character from several angles used to be sent whole to image-to-3D, which modelled every panel as one object. The model mirrors the vendor's: Hunyuan Pro takes **1 frontal image + 7 angles, 8 images max**, so the tab's **Input Image is the frontal image** and the turnaround group holds **companions only** — the Input Image is NEVER a group member. `mixie.moodboard_detect_views` (`ui/operators/detect_views_ops.py`) POSTs the tab's input image to `POST /api/v1/model-3d-generation/detect-views` (optional `hint` form field = the tab's Prompt) using its OWN compression profile `turnaround_detect` (4096px/q92 in `common/utils/image_compression_config.py`, the backend's hard ceiling — NOT the 2048px `image_to_3d` profile: the sheet gets split, so upload resolution divides down into the per-view 3D input and 2048 would cap a four-panel sheet at ~500px crops). On `is_turnaround: true` the crops are downloaded into `bpy.data.images` as `<base>_<view_type>` and appended to `mixie_moodboard_images` with the `view_type` / `turnaround_group` / `s3_key` props (`ui/moodboard_properties.py`), ingested by response `view_type`: **`main`** (always `panels[0]` — the front orthographic, falling back to the hero render) becomes the Input Image and is marked `turnaround_main_group = <group id>` (only when the sheet actually yielded companions), which is what binds the set to it; it never joins the group; **`hero`** (only when the sheet has BOTH) lands untagged beside it as an alternative Input Image for stylised sheets — no vendor `ViewType` describes it, so it can only be used by promoting it; everything else joins the group as a companion. Both main and hero KEEP their `s3_key`, so whichever is the Input Image submits by key instead of re-uploading pixels. `is_turnaround: false` reports "No multiple views detected" and changes nothing — it is NOT an error (a 502 IS: detection failed, credits refunded, retry; the call is credit-charged as `turnaround_detect`, so 402 flows through the shared `job_queue/core/error_helpers.classify_error`). **A set is BOUND to its frontal image by `item.turnaround_main_group`** (`ui/moodboard_properties.py`), set on the set's main image and NOWHERE else — companions and ordinary images leave it empty, and exactly one item per group ever carries it. `build_active_group_payload(scene, image, service_key, model_slug)` resolves the set FROM `image` via `group_id_for_main_image()`, so **a set applies only when the image being converted is that set's own main image**; anything else takes the plain single-image path. EVERY generate path goes through it — `mixie.model_gen_generate` and both of `mixie.image_to_3d_generate`'s (sidebar `execute` + direct-agent `_execute_direct`) — never `get_active_group` + `build_multi_view_payload` directly, and never pre-gated on `model_supports_multi_view` (an early-out there silently drops the set instead of raising, which is the degrade-to-one-image the guard exists to prevent). `service_key` is threaded from each operator's own resolved service (Model Gen Pro mode is `image_to_3d`, NOT `model_3d`), so capability is checked against the service actually being submitted. `tests/moodboard/test_turnaround_operator_wiring.py` pins all of this at source level, because operator classes cannot be exercised outside Blender (`bpy.types.Operator` is a MagicMock, so `class MIXIE_OT_x(Operator)` yields a mock and its methods are unreachable). This is deliberately a SECOND property rather than putting the main into `turnaround_group`: that one means "companion of" and `group_items` / `eligible_selected_images` / the 7-companion cap / the panel's view list all iterate on it, so the main joining the group would make it a companion of itself. (The backend reads both names through a bpy script — they are a frozen cross-repo contract.) `tab_image_to_3d.turnaround_group` (`ui/moodboard_tab_properties.py`) still names the set, but is now **UI state only**: which set the Multiple Views panel lists and grows. Reading the set off the TAB at submit time was a production bug — turn 1 detected a turnaround, turn 2 converted an unrelated image and inherited turn 1's companions, morphing the two subjects, and the guard's refusal made the agent retry then switch to the one multi-view engine it knew. `get_active_group()` and `group_id_for_main_image()` both self-heal to "" when the set has no companions left, and neither writes during draw. When a set is bound and the model cannot take multi-view, `build_active_group_payload` raises a TERMINAL `ValueError` naming the model, the view count and the main image, and telling the agent NOT to retry with a different model (it reaches the agent verbatim via `set_agent_gen_reason`) — never a silent degrade to one image. Logic is split four ways for the 500-line limit: `core/turnaround_views.py` owns the **group data model** (active-group + tab-input wiring, queries, `attach_image`/`detach_image`/`add_images_as_views`/`next_free_view_type`/`allowed_view_types`), `core/turnaround_binding.py` owns the **set ⇄ frontal-image binding** (`group_id_for_main_image`/`main_image_item`/`set_group_main_image`/`clear_group_main`/`forget_main_if_empty`, all re-exported from `turnaround_views`), `core/turnaround_payload.py` owns **`build_multi_view_payload(scene, group_id, main_image, model_slug)`** (Input Image → `image_s3_key` or `image_bytes_b64`; companions → `multi_view_images`, mixed key/base64 lists are legal — job_queue `uploads.py` stages only the base64-bearing entries, locked in backend-side by `tests/unit/modules/job_queue/test_payload_key_ownership.py`), and `core/turnaround_detect.py` owns the **detect-views network + ingest path**. The single **Multiple Views** section is drawn by the shared `ui/turnaround_drawer.py`, used by BOTH the catalog Model Gen drawer and the legacy Basic fallback: a header with the live `(N of 7)` counter, `Detect Views` and a group-wide X (`mixie.moodboard_clear_turnaround`); a 3-column grid of every companion with an editable angle dropdown, `^` promote and `X` remove; then **`Add Selected`** + a small secondary folder icon. **`Add Selected`** (`mixie.moodboard_add_selected_views`) is the primary add path — it takes every selected moodboard image that is not already a view and is not the Input Image, and **auto-assigns each the next unused angle** in `left, right, back, top, bottom, left_front, right_front` order, so the common left/right/back turnaround is one click with no dropdown work. It and the folder picker (`mixie.moodboard_add_turnaround_view`) are disabled at 7 rather than letting the job fail server-side. **`^` promote** (`mixie.moodboard_promote_turnaround_view`) makes a view the Input Image: it leaves the group (there is no vendor angle for a front view) keeping its `s3_key`, and TAKES the `turnaround_main_group` marker with it — the set follows its frontal image. The previous Input Image is demoted INTO the set at the angle the promotion just freed (it is a real view of the same subject, and promotion always frees exactly one slot); it stays an ordinary untagged board image only when it is not on the moodboard or no allowed angle is free. `Add Selected` and the file picker also bind the tab's Input Image as the new set's main — an unbound set is inert, so a hand-assembled one would otherwise silently never submit — and clearing a set (or removing its last companion) drops the marker so nothing dangles. That is how the `hero` gets used, and it replaces the deleted `main`/`front` dropdown entries. **View vocabulary** (`TURNAROUND_VIEW_TYPES` in `moodboard/constants.py`, frozen against the backend's `ViewType` enum): exactly the seven angles `left`/`right`/`back`/`top`/`bottom`/`left_front`/`right_front`. `main`, `front` and `none` were DELETED — `main` duplicated the Input Image slot, the vendor has no front slot, and `turnaround_group != ""` already means "in the set". `main`/`hero` survive only as detect-response vocabulary (`DETECT_PANEL_MAIN` / `DETECT_PANEL_HERO`), never as labels. **3.0 gating**: `top`/`bottom`/`left_front`/`right_front` are 3.1-only (`TURNAROUND_BASE_ANGLE_ONLY_SLUGS`). This is the ONE model-capability list still hardcoded, and deliberately so — a **KNOWN CATALOG GAP**: the backend's `public_model()` projection publishes no per-model view-type field and no Pro model's `parameters` schema carries a `view_type` enum, while `HunyuanAdapter` forwards `view_type` to Tencent unvalidated. So this client filter is the only thing preventing a vendor rejection *after* credits are held. Retire it by publishing supported angles per model (a schema `view_type` enum suffices — `generation_params.get_param_enum_items` already reads those); until then it is keyed on the slugs `hunyuan.core.pro_model.resolve_pro_model_slug` mints and must move with them. `view_type` is deliberately a STATIC enum — it is saved .blend data and a dynamic `items=` callback would remap or lose stored values when the model switches — so gating happens twice instead: auto-assignment never hands out an angle the model cannot take, and `build_multi_view_payload` drops invalid ones with a warning (plus an alert-red row and a hint in the panel) rather than submitting something the vendor rejects after credits are held.

**Submit sends S3 keys where it has them, pixels where it doesn't**: when the input image belongs to a `turnaround_group`, `mixie.model_gen_generate` and `mixie.image_to_3d_generate` send ONE job whose payload is `{image_s3_key: <main>, multi_view_images: [{s3_key, view_type}, ...]}`. Detect-views keys are passed VERBATIM (job submit validates key ownership per user). A view the user ADDED has no key, so it carries `{image_bytes_b64, filename, view_type}` instead (`image_bytes_b64` + `image_filename` when it is the main); the backend accepts a mixed list — `modules/job_queue/uploads.py` stages only the entries carrying `image_bytes_b64` and passes S3 keys straight through. `main` and `front` must never appear in `multi_view_images`. Main-count problems REFUSE to submit rather than degrade silently — zero mains and >1 main both raise, because the next step is a multi-minute ~50-credit job and quietly dropping a panel would build the model from less data than the user believes they supplied; duplicate companion view types are still collapsed with a warning (genuinely ambiguous, harmless). The section surfaces both main-count errors inline before the spend. `scene.hunyuan.pro.multi_views` is deliberately NOT consulted by the Model Gen tab any more — it belongs to the standalone Hunyuan panel (`modules/hunyuan/ui/hunyuan_panel.py` + `_submit_pro` in `hunyuan_ops.py`), which still drives it and is untouched. **Agent-drivable**: `mixie.moodboard_detect_views` takes `image_name` (resolves `bpy.data.images[image_name]` and ignores the moodboard selection entirely; a missing name errors and cancels rather than falling back to the selection, which would have the agent act on whatever the user last clicked), and `mixie.hunyuan_generate` takes `multi_view` (bool, default False) which on the PRO path resolves `image_name`'s group and submits it via the same `build_multi_view_payload()`, erroring if the image is in no group — explicit opt-in, so generating from one crop stays possible and intent is visible in the call. `multi_view` also counts as a direct-invocation param in the PRO routing condition, otherwise setting it without an `image_name` would silently fall through to the UI path. `enqueue_pro_job()` / `_build_pro_payload()` gained an optional keyword-only `turnaround` dict that REPLACES the inline image/multi-view bytes when set; every existing caller omits it and is unchanged. Because `poll()` cannot see operator properties, detect-views' poll no longer requires a selection (it would block agent calls) — the sidebar button's has-an-image gating moved into the drawer, and both it and the operator resolve the input through the shared `sidebar_ui_helpers.get_image_to_3d_input_image()`. Ungrouped images keep the existing `image_bytes_b64` single-image path unchanged. The button only renders when the selected model takes multi-view input — catalog flag `supports_multi_view` first, falling back to `TURNAROUND_FALLBACK_MULTI_VIEW_SLUGS` in `moodboard/constants.py` when offline/pre-auth. **Stage 2b converted the remaining tabs**: **Texture Gen** (panel `MIXIE_PT_gen_lookdev360`, bl_category "Texture Gen") — capability `texture_gen`, modes PBR Textures/`pbr_gen` (existing `mixie.lookdev360_generate` pipeline, resolution now schema-driven), Texture Edit/`hunyuan_texture_edit` (`mixie.texture_edit_generate` in `ui/operators/texture_gen_ops.py`: FBX export + reference image XOR prompt, shares `FEATURE_LOOKDEV360`), and Procedural Material/`mat_gen` (`mixie.texture_gen_matgen` → paint-side `enqueue_matgen_job`, catalog slug fast/detailed = pipeline); drawer `ui/texture_gen_drawer.py`. **Retopology** — capability `retopology`, drawer `ui/retopology_drawer.py` + `mixie.retopology_generate` (`ui/operators/retopology_gen_ops.py`) fanning out through `enqueue_retopology_jobs`; Tripo "Bake Textures" stays client-side on `scene.hunyuan.topology.tripo_bake`. **UV Unwrap** — capability `uv_unwrapping` (single service, Mode hidden): the catalog Model dropdown/params append to the existing drawer, submit flow unchanged. **Mesh Segment** — capability `mesh_segmentation` with modes `mesh_segment` (existing flow) and `hunyuan_part` (embeds the former PART_SEGMENT flow). **Scene Recon** — pipeline flags schema-driven from service `scene_reconstruction` (`_gather_recon_params` in `scene_recon_ops.py`); `save_to_library`/`asset_library_path` remain plain Blender props. Catalog Mode/Model enum selections for tabs without their own prop group live in `ui/moodboard_catalog_tab_props.py` (registered via `moodboard_scene_registration.py` before the sidebar container). Every tab keeps its legacy hardcoded UI as the catalog-not-loaded fallback. **Stage 3 — final sidebar** (`ui/moodboard_sidebar_panels.py`): exactly one N-panel per catalog capability, in catalog sort order — Image Gen, AI Render, Model Gen, Texture Gen, Scene Gen, Retopology, UV Unwrapping, Mesh Segmentation — plus the Queue utility panel (`mixie.queue_view` keys on its category). AI Render is catalog-only: unlike the seven legacy tabs it has no offline fallback, so its `poll()` requires a loaded catalog with `ai_render` services (empty/absent capability = hidden tab, which makes the depth_to_image move revertible from the DB alone). **Scene Gen** (panel `MIXIE_PT_gen_scene_recon`, `ui/scene_gen_drawer.py`) merges Scene Reconstruction (`scene_reconstruction`) and Segments to 3D (`scene_gen`) behind a Mode dropdown; each mode's operator/payload flow is untouched. Panel labels/categories are CATALOG-DRIVEN (`ui/moodboard_tab_labels.py`: resolved via `generation_catalog_cache.get_capability_label()` at registration — the bootstrap loads the persisted payload before the deferred UI batch — and `refresh_tab_labels()` re-registers only renamed panels from the cache's main-thread swap callback; hardcoded strings are offline fallbacks, bl_idnames never change, and category-switching code resolves tabs through `get_tab_category(capability_key)` — sidebar_ui_helpers, director handoff, onboarding tour — instead of literals) with catalog-driven content, and each panel's `poll()` hides the tab when the loaded catalog has no moodboard services for its capability; offline all 7 tabs render legacy fallbacks. Removed in Stage 3: the Blockout panel (absorbed into Image Gen), the Segment panel (merged into Scene Gen), the legacy single-panel tab strip (`moodboard_sidebar_panel.py`) + `core/sidebar_animation.py`, the sidebar `active_tab`/`imagegen_subtab`/`segmentation_subtab` enums (`image_to_3d_subtab` survives for the Model Gen offline fallback), and the dead hunyuan/part/rapid drawers in `sidebar_tab_drawers.py`. Segmentation tools (magic select, box/lasso mask) surface results via `sidebar_ui_helpers.focus_scene_gen_segments()`. **Auto Rig** (panel `MIXIE_PT_gen_animate`, bl_category "Auto Rig", `ui/animate_drawer.py` + `mixie.animate_generate` in `ui/operators/animate_gen_ops.py`) — catalog-only like AI Render (poll requires loaded catalog with `animate` services, no offline fallback): capability `animate`, single service `tripo_rig` (per-selected-mesh GLB export ≤150MB through `hunyuan/core/animate_enqueue.py`), on the client `FEATURE_ANIMATE` queue. Rigged glTF is imported with `guess_original_bind_pose=False` (`import_options` threaded through `enqueue_generation`) so Tripo rigs don't collapse. The Tripo retarget/animation mode is not seeded in the catalog (the backend seed `animate_catalog.sql` is Auto-Rig-only); the backend `tripo_retarget` adapter code remains but is unreachable. Agent-accessible: the backend `GENERATION_SPECS['tripo_rig']` (job_type `tripo_rig`) dispatches the client `mixie.agent_auto_rig` operator (`ui/operators/agent_auto_rig_ops.py`) with an explicit `object_name` — the RiggingAgent lane (`lanes/rigging`) is prompted to ALWAYS auto-rig via this tool and never hand-build armatures with scripts unless the user explicitly asks for manual rigging. All generate operator bl_idnames and `scene.hunyuan.*` property groups are unchanged (frozen agent contracts). |
| **hunyuan** | AI 3D generation (text/image → 3D mesh), retopology, UV unwrapping. Retopology offers two engines as **models of the one `retopology` backend service**: **Hunyuan** (`hunyuan_topology`) and **Tripo** (`tripo_v2`, v2.0 `mesh/decimate`); pre-merge catalogs still expose Tripo as the separate `retopology_tripo` service and the enqueue follows whichever service the catalog resolved. Both share the same client Retopology queue (`FEATURE_RETOPOLOGY`); the engine is chosen by the queue `job_type`/`model` sent to the backend, decoupled from the client `feature_key`. Also hosts the Auto Rig enqueue helpers (`core/animate_enqueue.py`): Tripo v3 auto-rig (`tripo_rig`) jobs for the moodboard Auto Rig tab (the retarget/animation path was removed from the catalog; dormant backend code remains). |
| **common** | Shared API clients (13 services), WebSocket infrastructure, notifications (server notifications with `action_url` + optional `action_label` render a primary link button — `NotificationAction.url` carries the URL through the toast click dispatch, opens via `wm.url_open`, and sends the read receipt; built in `store.push_from_server`), versioning, auto-updates (**browser-based only** — the update toast's [Download] opens the downloads page; there is NO in-app download/installer launch, that machinery was removed after MSI launch failures on Windows. Startup check, forced-update enforcement, Help → Check for Updates, and a persistent topbar "Update Available" badge right of Open Mixie — `updates/ui/topbar_badge.py`, drawn from agent_bubble's topbar hook; clicking re-shows the sticky update toast, badge persists after Skip/dismiss, and redraws are forced via the Mixar `Window.global_areas` RNA since the topbar is a global area), and `generation_params/` — the schema-driven parameter engine (see Generation Catalog section) |
| **auth** | OAuth PKCE flow with native keyring storage (macOS Keychain, Windows Credential Manager) |
| **asset_search** | Neural embedding-based asset library search and training |
| **mesh_segment** | UV mesh segmentation via SAM-based API |
| **texel_density** | UV texel density analysis and visualization |
| **uv_editor** | Advanced UV editing workspace with dual-space architecture, mutually exclusive tool/header panels, annotate and UV tool sidebars, dynamic panel ordering, and toolbar auto-expand |
| **space_texture_sets** | Texture set management |
| **Agent Scene Strip** (C++ region + `agent_scene_strip` Python keymap module) | Bottom-docked View3D region (`RGN_TYPE_EXECUTE`, `src/source/blender/editors/space_view3d/view3d_agent_strip*`): live offscreen-rendered viewport tiles of **every scene except the window's active one**, for monitoring parallel agents (one per scene) without leaving the viewport. Replaces the removed Scene Grid editor space (`SPACE_SCENE_GRID` = 109, enum value reserved). Poll-driven region: auto-shows when the file has more than one scene, auto-hides otherwise. Realtime via a 0.1s TIMERNOTIFIER poll + depsgraph change detection (`DEG_get_update_count`), so tiles follow script edits to non-active scenes with no notifiers needed. Per-tile orbit/pan/zoom (`VIEW3D_OT_agent_strip_*`), click-to-activate scene (swaps that tile out for the previous active scene), per-scene `mixie_chat_is_busy` badge; tiles follow the host viewport shading (solid/material). Mixar file subversion 100.3 adds the region to previously saved View3D areas. Key bindings live in `modules/agent_scene_strip/ui/keymap.py` (addon keyconfig): the GUI keyconfig preset reload wipes items from all C-registered keymaps in the default config, so C-side `WM_keymap_add_item` bindings go dead in GUI sessions — addon-keyconfig registration is the required pattern for custom C region keymaps (same as `space_mixie_chat`). |
| **operation_history** | Per-session local log (`operations.jsonl` + `scripts/`) of every agent script execution plus curated manual user ops. Agent executions captured at `space_mixie_chat/core/main_thread_executor.py`; manual ops via a depsgraph→timer capture service. Read by the agent through `operation_history/core/tools.py:run_tool`. No backend DB. |
| **testing** | Standalone stable pytest suite runs by default; the legacy/embedded module suite is explicit because it requires Blender/runtime dependencies. |

**Safety contracts:** Mixie Chat's Generate options are backend-authoritative: a successful empty list fails closed to a `NONE` placeholder and disables generation; never resurrect hardcoded services. Feedback becomes permanently locked/received only after a confirmed 2xx; enqueue or delivery failures enter retryable `FAILED` and preserve comment text. BYOK API keys/Codex bundles are transient `SKIP_SAVE` fields wiped on dialog exit, successful save/remove, truncation, and unregister, and catalog models are validated against the selected provider. Unified queue terminal states release large transient payloads/result URLs while retaining lightweight history; scene activity flags are recomputed per originating scene.

**Director safety contract:** shots reference native Blender scene/camera/timeline/output state instead of copying it into a second editable source of truth. The C++ surface only reads registered Director RNA and invokes Python operators, so directing behavior remains centralized in the module instead of being duplicated natively. The camera selector switches to the newest Shot take already owned by that camera or creates a Shot; it must never reassign the active Shot's camera and collapse separate camera timelines. Active-shot changes keep the scene camera and preview range synchronized. The live camera gate owns placement and state display for Flow-style FOV, aspect, and Navigate/Precise controls; preset application stays in Python operators and writes native camera/render properties. Locking preserves the compiled `snapshot_json`; further edits happen in a new child take. Shot-video renders must preserve and restore the user's camera, frame range/current frame, renderer, output, shading, color-management, depth-pass, and compositor state; Blender 5 output must switch `image_settings.media_type` to `VIDEO` before selecting `FFMPEG`, and movie files must move from temporary output into persistent Mixar storage before shot-grouped Moodboard insertion. Video handoff must remain catalog-limited and model-neutral.

---

## Unified Job Queue

All AI generation features (image gen, 3D gen, retopology, UV, lookdev, brush gen, scene gen) submit through **one unified async job queue** instead of bespoke per-feature services. Lives in `modules/common/job_queue/`.

**Backend contract**: `POST /job-queue/jobs` (job_type, model, payload) returns the submit acknowledgement, then the shared agent WebSocket drives state via compact `job.update` pushes and full `job.get` / `job.sync` reconciliation (the watchdog requests sync; it does not run a recurring REST poll). Client views include the submitted `model`; missing model fields from older backends must not erase the locally submitted value.

**Client architecture**:
- **`core/job.py`** — base `Job` with shared model display metadata, optional catalog-backed `origin_capability_key` for composite workflows, missing-safe backend metadata merging, `_unwrap_response()`, `_parse_standard_submit()`, `_parse_standard_poll()`, and default `poll()`.
- **`core/generic_jobs.py`** — two generic Job classes cover all standard features:
  - `AsyncGLBJob` — submit → poll → download GLB → import (3D gen, retopology, UV, part, rapid, image-to-3D, scene gen HP/LP). Optional `on_imported` hook for rename/chain-id stamping.
  - `SyncImageJob` — submit with inline result → download images to moodboard (image gen, lookdev/depth-to-image, brush gen). Honors an optional backend-echoed `image_name` in the job result — set ONLY when the caller chose a name (the agent flow's `_execute_direct` sends `payload["image_name"]`; the backend sanitizes it, uses it as the S3 filename base — slugged + short request id + index — and echoes it back). There is deliberately NO model-generated naming: Gemini image calls are IMAGE-only-modality with no fallback second call — TEXT modality risks text-only no-image responses (backend commit 6f16c5b), and live probing showed in-call naming is a dead end anyway (`gemini-3.1-flash-image-preview` never returns a text part regardless of config; `gemini-2.5-flash-image` obeys a naming instruction by dropping the image). Datablock name priority: `base_name`/echoed `image_name` > S3 filename stem (e.g. `golden-dog_4c576091_01`, prompt-derived and unique per image) > legacy `{name_prefix}_{timestamp}_{i}` only when the URL carries no usable filename — so datablock name, packed filepath, and S3 key stay aligned. Downloaded generation images also keep the server-side S3 file name: the temp file is written under the URL-path basename (`download_image_to_tempfile(filename=...)` in a private `mixar_img_*` temp dir, cleaned by `cleanup_temp_image`) and after packing the datablock's `filepath_raw` is set to that name as a blend-relative phantom path (`//<s3name>.png`) instead of being blanked — the pixels stay packed, nothing re-reads the deleted temp file.
- **`core/enqueue.py`** — single `enqueue_generation(kind="glb"|"image", ...)` entry point. Builds the right Job, auto-attaches the queue listener (custom or from `scene_flag`), submits.
- **`core/helpers.py`** — `get_queue_with_listener()`, `create_scene_flag_listener()` (with `on_start`/`on_finish` hooks), `show_batch_summary_popup()`, `extract_image_urls()`, `download_images_to_moodboard()`.
- **`core/queue_manager.py`** — `FeatureQueue` drives Job lifecycle: PENDING → RUNNING_SUBMIT → RUNNING_POLL → RUNNING_DOWNLOAD → SUCCESS/FAILED. The RUNNING_DOWNLOAD→terminal half lives in `core/queue_download.py` (`DownloadMixin`, same class, separate file for the 500-line limit). The unified queue row joins backend `service`/`model` keys to generation-catalog capability and model labels. Exact raw identifiers are used only when the catalog is unavailable or no longer contains a historical row. Composite workflows such as Scene Gen HP/LP carry `origin_capability_key=scene_gen`, so the badge follows the backend's current Scene Gen label even though the submitted execution service belongs to another capability. Jobs whose stable `label` also carries dedup/downstream naming data provide a separate `display_label`; never parse prompt/object text to remove prefixes. Do not add client-side generation naming maps. **Queue-activity feedback**: one toast under one stable id (`core/enqueue_toast.py`) tracks the queue from first enqueue to drain, because the agent routinely enqueues a multi-minute paid generation, answers in chat and drops to IDLE — after which nothing on screen said work was outstanding. While any job is pending/running/paused it is **STICKY** ("3 generations in progress" + View Queue, `QUEUE_ACTIVE_TOAST_TTL_MS = 0`); when the queue drains it is REPLACED by a transient "N generations ready" summary (`QUEUE_READY_TOAST_TTL_MS`), or simply dismissed when nothing succeeded (every failure already raised its own toast in `_notify_failure_toasts`). Counts are **derived from live queue snapshots** on every `_notify()`, never accumulated in a burst counter — that is what keeps them right when jobs are enqueued minutes apart, fail while others run, or are cleared underneath the toast. Two disciplines the implementation depends on: the toast is only re-pushed when its rendered text CHANGES (`_notify()` also fires on the 0.5s download-progress tick, and a push replaces the store item wholesale), and a user dismissal is detected via `NotificationStore.contains()` — a sticky item never expires, so its absence after a push can only mean the user closed it — and respected until the next `submit()`. `mixie.queue_view` is context-safe and space-aware: toast action buttons fire from a `bpy.app.timers` callback with no `context.area`, and the Queue panel registers in the MIXIE space (not VIEW_3D) — the operator targets the largest area of the panel's actual space type (`QUEUE_AREA_TYPE`) whenever the invoking context isn't already one.

**Result download policy** (`core/downloader.py` — `download_file()`, split out of `model_io.py`; all knobs in `job_queue/constants.py`): a finished job used to sit in "Downloading…" indefinitely holding a concurrency slot, because `urlopen(timeout=...)` is a *per-read* socket timeout that any trickle of bytes resets, and `_fail_timed_out` hard-returned on every state but `RUNNING_POLL`. Four rules now hold. **(1) Total deadline** — `DOWNLOAD_TOTAL_DEADLINE_S` (600 s) bounds the whole transfer across every retry, checked between chunks, with the per-read socket timeout capped by the remaining budget so one wedged read can't overshoot; `DOWNLOAD_SOCKET_TIMEOUT_S` is kept separate from `api/constants.HUNYUAN_TIMEOUT` (that one budgets a submit upload). A main-thread backstop lives in the 30 s sync watchdog: `RUNNING_DOWNLOAD` is in `_WATCHDOG_STATES` but deliberately NOT in `_BACKEND_SYNC_STATES` (nothing left to reconcile — the backend job is already DONE), and `_fail_timed_out` now branches per phase, never cancelling the backend job for a download failure since the generation was already produced and paid for. **(2) Completeness** — `Content-Length` is compared against bytes written and a mismatch raises "incomplete" with both numbers; absent header keeps read-until-EOF and claims no verification. Every failure path unlinks the temp file. **(3) Bounded retry** — `DOWNLOAD_MAX_ATTEMPTS` (3) with 2 s/4 s backoff for connection errors, read timeouts, 5xx/408/429 and truncation; a 4xx is never retried (presigned S3 URLs expire — a 403 tells the user to rerun the job, and the client must never re-derive or refresh the URL), and the retry loop shares the deadline rather than extending it. **(4) Progress** — `Job.download_bytes`/`download_total_bytes`/`download_attempt` render through `job.py:download_substate_text()` as `Downloading… 12.4 / 48.0 MB`. **Threading contract**: `_bg_download` is a daemon thread — its `on_progress` callback writes plain ints onto the `Job` and touches neither `bpy` nor `_notify()`; a main-thread `bpy.app.timers` tick (`_start_download_progress_timer`, 0.5 s, only when the count moved) calls `_notify()`, because the UIList reads `substate_text` off the WindowManager mirror and only `_notify()` rebuilds that mirror — the blink pump redraws but does not re-sync. Cancel is noticed between chunks. Bespoke-queue jobs that own `handle_result` are outside this path and outside the watchdog (they never stamp `download_started_at`). Tests: `tests/test_job_queue_download.py`, including `ast`-level pins on the threading contract.

**Per-feature enqueue helpers** (build payloads + fan-out, then call `enqueue_generation()`): `moodboard/core/generation_enqueue.py` (Pro, scene gen HP/LP), `hunyuan/core/{retopology,uv,part}_enqueue.py`. **No model slug in this layer is a literal.** The Pro paths take theirs from `hunyuan/core/pro_model.resolve_pro_model_slug(model_version)` — THE one place the version→slug map lives (it was inline in three call sites, and since the turnaround angle gating keys off that slug, a fourth caller spelling it differently silently disables the gate); the Scene Gen LP retopology fan-out resolves its engine with `catalog_default_model("retopology")` and aborts with a user-facing report when the catalog cannot answer, instead of the old hardcoded `model="hunyuan_topology"`. Operators call these or `enqueue_generation()` directly — **do not** create new Job subclasses for standard features. `retopology_enqueue.py` branches on the shared snapshot's `model` key (`hunyuan`/`tripo`, set by the legacy Topology prop or the catalog tab): Hunyuan → `service=retopology, model=hunyuan_topology`, `params` `polygon_type`/`face_level`/`post_process`; Tripo → `model=tripo_v2`, `params` `quad`/`face_limit`/`bake` (backend clamps face_limit), a 150 MB export cap, and an import hook that renames to `*_low` and only Smart-UV-unwraps when `bake=false`. Tripo submits to `shared["service_key"]` — the merged `retopology` service on current catalogs, falling back to the legacy `retopology_tripo` service (pre-merge catalogs / legacy props path). `shared["model_slug"]` overrides the model constants when the catalog tab selects one.

**Genuinely unique jobs keep their own queue files** (custom polling/result handling): `lookdev360_queue.py` (PBR textures → fill layers), `scene_recon_queue.py` (progressive 2-phase), `scene_gen_exp_labels_queue.py` (label extraction), `mesh_segment_queue.py` (inline JSON → vertex groups), `matgen_queue.py` (inline script → procedural material).

---

## Generation Catalog & Dynamic Params

Foundation for the DB-driven moodboard tabs — the existing generation tabs retain their legacy offline fallbacks, while catalog-only capabilities such as Video Gen are hidden until the backend publishes an enabled service/model and have no hardcoded submit fallback.

- **`bootstrap/generation_catalog_cache.py`** — owns lock-guarded lifecycle and ETag revalidation for `GET /api/v1/generation-catalog` (capabilities → services → models → parameter schemas, plus styles and credit costs). Disk I/O lives in `bootstrap/generation_catalog/storage.py`; read-only typed lookups live in `bootstrap/generation_catalog/queries.py`. Keeping these pure helpers in a subpackage leaves the lifecycle module below the 500-line limit and prevents the top-level bootstrap loader from executing them twice. The cache uses 2s-delayed background fetch, logout clear, manual refresh, and persisted stale data (`generation_catalog.json` in `bpy.utils.user_resource('DATAFILES', path='mixar')`) so panels render instantly on launch. Public accessors include `get_capabilities/get_capability/get_capability_for_service/get_services(capability, surface)/get_models/get_model/get_styles/get_credit_cost/is_loaded` plus enum-item helpers with LOADING/ERROR placeholders. Services carry `surface` (`moodboard`|`paint`) — paint-only services must never appear in moodboard tabs.
- **`modules/common/generation_params/`** — schema-driven parameter engine: dynamically builds one PropertyGroup per (service, model) from catalog schemas, attached to **WindowManager** pointers (not Scene) so re-registration on catalog change is safe (no .blend/undo persistence; rebuilds run on a main-thread timer scheduled by the cache). `draw_service_params(layout, service, model)` renders widgets by schema `widget` kind honoring `order`/`group`/`visible`/`visible_if`; `collect_params(service, model)` returns visible params as a typed plain dict for payloads. Never hardcode param names here — key sets change from the DB without client releases. Design rationale in `core/engine.py` docstring. Two more layers live here: **`core/selector.py`** — capability mode selector (`get_service_enum_items()` Mode-dropdown items with LOADING/ERROR placeholders, `resolve_service_key()` mode-enum→valid service key, `draw_capability_selector()` Mode→Model→params rendering that returns False when the catalog isn't loaded so tabs fall back to legacy UI); and **`core/assemblers.py`** — the generic wire contract: `assemble_payload(service, params, payload, model_slug)` puts `collect_params()` output (plus `prompt` where a service takes one) into `payload["params"]` (snake_case, None values dropped) for EVERY service; file/image inputs stay top-level (`image_bytes_b64`, `file_bytes_b64`, `multi_view_images`, ...). The backend provider adapters own all vendor mapping (Tencent PascalCase `sdk_params`, Tripo decimate body + face-limit clamping, fal snake_case) — never build provider shapes client-side. `resolve_model_slug()` (selector.py) maps a model enum value to a valid catalog slug with a hardcoded fallback. **`catalog_default_model(service)`** (selector.py) is its fallback-less sibling for submit paths that have no model dropdown to resolve from: it returns the service's catalog default, or `None` when the catalog cannot answer (not loaded / service disabled / no enabled rows). `None` means **abort the submit** — never substitute a literal slug. Model rows are server state that changes without a client release, so a remembered slug does not fail loudly, it burns a queue slot and the user's wait on a 422. Used by `scene_gen_queue.enqueue_scene_gen_job` and the Scene Gen LP retopology fan-out; both refuse (and report) rather than guess. Bespoke-queue services (`pbr_gen`, `mesh_segment`, `scene_reconstruction`, `mat_gen`) have no assembler entry: their Jobs build payloads from dataclass fields, so catalog params thread through the enqueue helpers' kwargs instead.
- **Legacy caches retired** — `bootstrap/imagegen_cache.py` / `model_3d_cache.py` were deleted (Stage 3); every consumer (moodboard enum callbacks, space_mixie popup props, paint brush-gen models, auth login/logout hooks, refresh operators, default-model/max-refs lookups) reads the generation catalog cache, with static hardcoded lists as the last-resort offline/pre-auth fallback. `generation_params.get_param_enum_items(service, model, param)` serves legacy enum props that mirror a schema param (e.g. aspect_ratio/resolution) outside the dynamic engine.

---

## AI Agent System

The backend runs a **LangGraph-based orchestrator agent** (Claude Sonnet 4.6 primary, Gemini 3.1 Pro fallback) with:

- **18 tool domains** and **200+ tools** covering modeling, texturing, UV, rigging, particles, scene management, layer painting
- **12+ workflow modes** (MODELING, TEXTURING, RIGGING, UV_UNWRAP, SCENE, LAYER_PAINTING, etc.) that filter which tools the LLM sees
- **Tool execution pattern**: LLM calls tool → backend validates → sends Blender script via WebSocket → script uses `__PARAMS__` for input and emits `__RESULT__` JSON → result fed back to LLM

---

## C++ Customizations (~150 files)

- **26 C++ files** for native chat UI rendering (markdown, thinking visualization, hit testing, thumbnails)
- **Authentication**: Cross-platform keyring + local OAuth PKCE callback server
- **Custom editor spaces**: `space_mixie_chat`, `space_mixar_properties`, `space_mixar_layers`, `space_mixar_assets`
- **3D viewport enhancements**: 13 modified files
- **ID-delete vs preview-job race**: `makesrna/intern/rna_main_api.cc` overlay — `rna_Main_ID_remove` (every `bpy.data.*.remove()`) calls `ED_preview_kill_jobs_for_id` before freeing the ID. Icon-preview wmJobs read the LIVE datablock on the job thread (images are never duplicated for the job — `duplicate_ids()` in render_preview.cc), and upstream only kills preview jobs on editor-operator/undo paths, so Python-side deletion (moodboard delete/clear-turnaround/promote) segfaulted in `do_job_thread` when thumbnails were still rendering.
- **Python GIL safety**: `python/generic/py_capi_utils.cc` overlay — `PyC_IsInterpreterActive()` uses per-thread `PyGILState_Check()` instead of the process-global current-tstate check. On Python ≤ 3.11 the global check answers "does *any* thread hold the GIL", which let C++ operators (GIL released around `WM_operator_call_py`) call Python C-API concurrently with background Python threads (auth/keyring, cache fetchers) → allocator corruption → startup segfaults in `PyUnicode_New`.

---

## Bootstrap & Loading

A sophisticated **two-phase registration system**:
1. **Synchronous bootstrap** (6 modules): paint property chains, agent connection, splash, caches, update checker
2. **Deferred UI loading**: ~415 Python files loaded in **4ms/frame time-budgeted batches** to keep Blender responsive during startup, with dependency-ordered priority (properties → operators → panels)

---

## Key Patterns

- **Overlay mechanism**: Mixar code overlays upstream Blender, making version upgrades cleaner
- **Singleton + daemon threads**: ConnectionManager for persistent WebSocket
- **Handler pattern**: Depsgraph handlers set flags → timers do work (avoids blocking draw)
- **Script communication**: `__PARAMS__` in, `print("__RESULT__" + json.dumps(...))` out
- **Headless sandbox supervision**: Parent Mixar process spawns a background sandbox child with platform-specific process flags; Windows children use Win32 process APIs for parent liveness checks.
- **Time-budgeted loading**: UI modules load without blocking the main loop

---

## Summary

This is a **production-grade, AI-augmented 3D creation platform** built on top of Blender. The texture painting system is the core feature (layer-based, node-driven, with procedural materials and baking), but it's surrounded by a rich ecosystem of AI capabilities — an autonomous chat agent that can manipulate the 3D scene, AI-powered 3D generation, reference moodboards, smart asset search, and mesh segmentation. The codebase is modular (~1,000+ Python files, ~150 C++ files) with clear separation between Blender-side UI/logic and backend AI services.
